name: Destroy VPC Infrastructure
on: 
  workflow_dispatch: 
jobs: 
  DEV: 
    runs-on: rhel_arm 
    steps: 
      - uses: actions/checkout@v4 
      - name: Destroying VPC Infrastructure 
        run: |
          ls -la 
          dir="/action-runner/_work/aws-devops/aws-devops/"
          cd $dir
          pwd 
          ls -la
          